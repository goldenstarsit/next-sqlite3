import type { StrategyManifest } from "../core/types";

export const dcaManifest: StrategyManifest = {
  id: "dca",
  name: "Dollar Cost Averaging",
  version: "1.0.0",
  description:
    "Configurable DCA trading strategy with exchange-aware order sizing.",
};
